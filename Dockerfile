# Заготовка образу. Добудовується на ЛР6, до того `make build` падає навмисно.

FROM python:3.12-slim

WORKDIR /app

# TODO ЛР6: скопіювати опис залежностей і поставити їх окремим шаром, щоб
#           зміна коду не перезбирала весь образ.

COPY src/ ./src/

# TODO ЛР6: конфігурація має приходити зі змінних оточення, а не з файлу .env,
#           запакованого в образ.
# TODO ЛР6: процес усередині образу не повинен працювати від root.
# TODO ЛР6: додати EXPOSE і CMD, який піднімає uvicorn на 0.0.0.0.

# TODO ЛР6: приберіть цей рядок, коли образ справді збирається і запускається.
RUN echo "Заготовка Dockerfile не добудована. Це завдання ЛР6." && exit 1
